import { RiInstagramFill } from "react-icons/ri";
import { IconTypes } from "@/types/globals";

const InstagramIcon = ({size, color}: IconTypes) => {
    return ( 
        <RiInstagramFill size={size} color={color} />
     );
}
 
export default InstagramIcon;