import { FaLinkedin } from "react-icons/fa";
import { IconTypes } from "@/types/globals";

const LinkedInIcon = ({size, color}: IconTypes) => {
    return ( 
        <FaLinkedin size={size} color={color} />
     );
}
 
export default LinkedInIcon;