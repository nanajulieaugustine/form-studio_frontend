import { MdOutlineFacebook } from "react-icons/md";
import { IconTypes } from "@/types/globals";

const FacebookIcon = ({size, color}: IconTypes) => {
    return ( 
        <MdOutlineFacebook size={size} color={color} />
     );
}
 
export default FacebookIcon;